source 'https://rubygems.org'

gem 'cocoapods'
gem 'fastlane'

plugins_path = File.join(File.dirname(__FILE__), 'BuyPolish', 'fastlane', 'Pluginfile')
eval_gemfile(plugins_path) if File.exist?(plugins_path)
