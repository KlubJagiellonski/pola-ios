import Quick
import Nimble
@testable import SHOWROOM

class ShowroomTests: QuickSpec {
    override func spec() {
        describe("testSpec") {
            it("test it") {
                expect(10).to(equal(10))
            }
        }
    }
}
