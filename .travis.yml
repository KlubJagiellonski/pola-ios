language: objective-c

xcode_workspace: Pola.xcworkspace
xcode_destination: platform=iOS Simulator,OS=12.4,name=iPhone 8
osx_image: xcode10.3

before_install: cd BuyPolish

jobs:
  include:
    - stage: test
      name: "Unit tests"
      xcode_scheme: Pola
    - name:  "UI tests"
      xcode_scheme: PolaUITests

notifications:
  email: false
